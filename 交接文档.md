# notblog-ui 前端交接文档

> 记录本次「UI 优化 + JavaScript → TypeScript 迁移」的所有修改，供后续接手者快速定位。
> 日期：2026-08-24（提交分支 `fix/api-contract-alignment`）

---

## 一、本次改动概览

1. **前端 UI 优化**（依据 frontend-design 设计规范，主题为「轻芒 · 兴趣部落」暖琥珀 → 珊瑚）
2. **JavaScript → TypeScript 全量迁移**（39 个纯 JS 模块 + 62 个 Vue SFC）
3. **清理冗余 / 无用代码**，保证代码简洁
4. 构建验证通过，已本地提交

---

## 二、TypeScript 迁移

### 2.1 基础设施

| 文件 | 说明 |
| --- | --- |
| `tsconfig.json` | 新增。`strict: false` 以避免存量类型债务阻塞构建；`@/*` 路径别名；`noEmit`（类型检查交给 vue-tsc） |
| `src/env.d.ts` | 新增。声明 Vue SFC 组件、环境变量（`import.meta.env`）、静态资源导入的类型 |
| `src/types/index.ts` | 新增。共享业务类型：`ApiResponse<T>`、`TokenResult`、`CurrentUser`、`PageParams` 等 |
| `.prettierrc.json` | 新增。统一代码风格（semi、singleQuote、2 空格缩进等） |

### 2.2 依赖变更（`package.json` / `vue.config.js`）

- 新增依赖：`typescript@^5.9.3`、`vue-tsc@^2.2.12`、`@vue/cli-plugin-typescript@~5.0.0`、`@babel/preset-typescript`、`@vue/devtools-api`
- `scripts`：新增 `"type-check": "vue-tsc --noEmit"` 用于独立类型检查
- `vue.config.js`：
  - `resolve.extensions` 增加 `.ts` / `.tsx`
  - webpack `ts-loader` 设为 `transpileOnly: true`，**构建仅转译不做类型检查**（类型检查统一走 `vue-tsc`），避免存量类型债务阻塞构建
  - 删除 `fork-ts-checker` 插件
  - `lintOnSave: false`

### 2.3 模块迁移清单

- **utils**（9 个）：`auth.ts`、`avatar.ts`、`drafts.ts`、`format.ts`、`image.ts`、`markdown.ts`、`notifications.ts`、`response.ts`、`skinStyle.ts`
- **api**（17 个）：`auth.ts`、`admin.ts`、`background.ts`、`chat.ts`、`circle.ts`、`comment.ts`、`danmaku.ts`、`follow.ts`、`markdown.ts`、`notification.ts`、`publish.ts`、`report.ts`、`space.ts`、`topic.ts`、`tweet.ts`、`userinfo.ts`、`video.ts`
- **stores**（9 个）：`auth.ts`、`call.ts`、`chat.ts`、`feedTab.ts`、`skin.ts`、`theme.ts`、`toast.ts` 等（Pinia store 全部拿到精确类型）
- **axios**：`src/axios/index.ts`（补全 Axios 实例与拦截器类型）
- **socket**：`callSignalR.ts`、`signalr.ts`（SignalR 连接与事件类型化）
- **router / main / navItems**：`router/index.ts`、`main.ts`、`layout/navItems.ts`
- **Vue SFC**（62 个）：script 块统一改为 `<script setup lang="ts">`，补充组件/事件/props 类型

### 2.4 类型实践要点

- 后端响应统一用 `unwrap(res)` 解构（`src/utils/response.ts`），兼容 mock 单层、真实 `ApiResponse<T>`、旧式双层三种形态；返回 `any` 以宽松方式消费，避免过于收紧产生爆炸式改类型成本
- 背景/蒙层样式返回类型使用 Vue 的 `CSSProperties`（`BackgroundLayer.vue`、`SkinPanel.vue` 等）
- `notification.ts` 中 `NotificationMeta` 补 `export` 供外部引用

---

## 三、UI 设计优化

### 3.1 设计方向

「轻芒 · 兴趣部落」：**暖琥珀 → 珊瑚** 的烘焙般温暖、清透质感，衬线宋体标题 + 系统无衬线正文。

### 3.2 移除 daisyUI

- 移除全部 daisyUI 依赖与配置：`package.json` 依赖、`tailwind.config.js` 的 `require("daisyui")` 插件、`input.css` 的 `@plugin "daisyui"` 及 daisyUI 专属圆角变量 `--radius-selector/field/box`
- 项目配色本就全部使用 Tailwind 工具类，未引用任何 daisyUI 颜色变量（如 `--primary/--base-100`），因此移除无损
- 暗色主题由 `input.css` 自定义的 `data-theme` 选择器 + `@custom-variant dark` 接管，`theme.ts` 只需维持 `data-theme` 属性即可，不含 daisyUI 专属机制
- `glass` / `glass-card` 为项目自定义 `@utility`，不受影响；`--radius-*` 变量在项目内无消费方，一并清理
- 收益：生产 CSS 由 172.92 KiB → 114.47 KiB（直观截除约 58 KiB），无运行时行为变化

### 3.3 主要改动（`src/input.css`）

| 项 | 说明 |
| --- | --- |
| 品牌色变量 | `--qm-brand`(amber-500)、`--qm-brand-strong`(orange-600)、`--qm-accent`(emerald-500)、`--qm-ink`、`--qm-paper`，随 `[data-theme=dark]` 联动 |
| 品牌字体 | `.font-display`：`Songti SC / Noto Serif SC / STSong / SimSun` 衬线宋体，标题/品牌处使用 |
| 应用背景 | 纯 CSS 多层径向/线性渐变（奶白纸张 + 暖橘/薄荷微光），`background-attachment: fixed`，浅深色两套；无图片开销 |
| 滚动条 | 10px 圆角暖色细滚动条，`::selection` 琥珀高亮 |
| 聚焦态 | `:focus-visible` 统一品牌色（amber）光晕，键盘可达 |
| 卡片交互 | `.card-lift` hover 上浮 + 柔和投影 + 轻触缩反馈 |
| 页面入场 | `.page-enter` 上浮淡入动画 `qm-page-in` |

### 3.4 关键组件改动

- `TopBar.vue`：标题加 `font-display` 品牌字体
- `LoginPage.vue`：Logo/主标题「轻芒 · 兴趣部落」加 `font-display`
- `BackgroundLayer.vue` / `SkinPanel.vue`：样式类型改用 `CSSProperties`

### 3.5 既有约束保留

- **纯色卡片**：`.glass` / `.glass-card` 类名不变（全项目约 120 处引用不动），仅去掉毛玻璃、背景提到 95% 不透明
- 圆角统一 10px（`.rounded-[5%]` 规则强制）
- `dark:` 变体跟随 `data-theme` 而非系统偏好（`@custom-variant dark`）
- 暗色模式文字统一纯白以提升对比度

---

## 四、清理内容

- 删除全部 `src` 下的 `.js` 源文件（39 个），以对应 `.ts` 替代
- 配置消除：移除 `fork-ts-checker`，构建不再阻塞于类型检查

---

## 五、构建与验证

```bash
npm install          # 安装 TS 相关依赖
npm run build        # 生产构建 ✅ 通过（仅 asset size / browserslist 旧数据提示）
npm run type-check   # 独立类型检查（vue-tsc）
```

> 构建仅 warnings（chunk 体积超 244 KiB、browserslist 数据较旧），不影响产物生成。

---

## 六、后续建议

1. 结合现状逐步收紧 `tsconfig`：`strict: true` 需要逐轮清理 `any`（尤其 `response.ts` 的 `unwrap`）
2. 大体积资源（如 `120498537_p0_master1200.jpg` ~741 KiB）建议走 CDN 或压缩
3. `input.css` 中的品牌视觉（字体、背景、动效）如需调整，改此处即可全局生效，注意与 daisyUI 类名冲突（`.glass` 已手动覆盖）